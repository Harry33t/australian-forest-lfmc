# F2 数据准备 + RF baseline 依赖(W1 这几个就够;SSL/Transformer 训练在 GPU 机另装)
numpy>=1.24
pandas>=2.0
openpyxl>=3.1            # 读 Globe-LFMC 2.0 的 .xlsx(72.5MB,多 sheet)
pyarrow>=14             # 把 LFMC Data sheet 转 parquet(读一次很慢,缓存提速)
scikit-learn>=1.3       # RandomForest 回归 baseline + 分层抽样
tqdm>=4.66
geopandas>=0.14         # (W3)经纬度空间 join IBRA7 → bioregion,做 leave-one-region-out
# --- 卫星特征阶段(DEA / Digital Earth Australia,不在此 pin)---
# datacube / odc-stac / pystac-client  → 见 src/satellite_dea.py(W2 接入)
# --- 训练 / 不确定性阶段(GPU 机)---
# torch / mapie(conformal)  → W2/W3
